spring:
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: update

logging:
  level:
    root: info
    jdbc: warn

log4jdbc:
  dump:
    sql:
      addsemicolon: true
      maxlinelength: 0
  sqltiming:
    warn:
      threshold: 50
    error:
      threshold: 300
